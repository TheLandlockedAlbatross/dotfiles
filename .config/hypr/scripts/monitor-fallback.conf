# Primary display to fall back to when all external displays disconnect.
# Set to a DRM connector name (e.g. eDP-1, DP-3) to override auto-detection.
# Leave empty to auto-detect the first eDP connector.
PRIMARY_DISPLAY=
