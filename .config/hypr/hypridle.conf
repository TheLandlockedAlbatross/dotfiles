general {
    lock_cmd = ~/.local/bin/lock-screen                    # lock screen and 1password; password box on focused display
    before_sleep_cmd = OMARCHY_LOCK_ONLY=true ~/.local/bin/lock-screen    # lock before suspend without scheduling display off.
    after_sleep_cmd = sleep 1 && omarchy-system-wake                 # delay for PAM readiness, then turn on display.
    inhibit_sleep = 3                                      # wait until screen is locked
}

# Start screensaver at 10 minutes (all displays; omarchy-launch-screensaver
# walks every monitor).
listener {
    timeout = 600
    on-timeout = pidof hyprlock || omarchy-launch-screensaver
}

# Lock at 15 minutes of real idle (screensaver launch at 600s resets the idle
# timer, so 600 + 300 = 900s wall time). LOCK_ONLY keeps the displays on for
# the 5 seconds until the shell lock's own blank timer (shell.json idle.lock)
# turns them off; that timer re-arms on every keystroke while locked, so the
# screens come back for typing and go dark again 5s after it stops.
listener {
    timeout = 300
    on-timeout = OMARCHY_LOCK_ONLY=true ~/.local/bin/lock-screen
    on-resume = omarchy-system-wake
}

# Screens off at 15:05 of real idle (600 + 305), belt-and-braces with the
# shell lock's 5s post-lock blank which fires at the same moment. Suspend is
# never automatic — hypridle has no suspend listener and logind
# IdleAction=ignore; only SUPER+SHIFT+CTRL+L (or a physical suspend key)
# sleeps the machine.
listener {
    timeout = 305
    on-timeout = omarchy-brightness-keyboard off; omarchy-brightness-display off
    on-resume = omarchy-system-wake
}
